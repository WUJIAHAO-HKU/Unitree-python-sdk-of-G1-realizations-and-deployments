# 动捕服数据 → 机器人残差学习 思维导图

## 1. 目标澄清
- 现有控制器：能力盘点、缺陷定位、性能指标（稳定性/能耗/柔顺）
- 残差学习目标：补偿量形式（力矩/速度/末端力）、期望场景（站立、走、上下台阶、扰动）
- 采集约束：机器人可实现 DOF、通讯/延迟限制、安全策略

## 2. 动作与场景规划
- 动作库：静止、步态、转弯、上下台阶、跨越、推拉POSE、跌倒恢复等分级强度片段
- 场景多样性：室内外、不同地面摩擦、高度差、随机扰动（拉绳、推杆）、附加载荷
- 执行者多样：不同身高/体重/运动风格；每次录制记录 `subject_id`、身材参数、装备状态
- 元信息：动作编号、执行者、设备状态、环境标签、地形参数（坡度/材质）、天气/温度（若户外）
- 记录模板：`{episode_id, motion_id, subject_id, equipment, env_label, notes}`，便于数据库化管理

## 3. 数据采集通道
- 动捕服骨架：姿态、关节角/角速、末端位姿、质心、接触flag（若系统支持压力垫）
- 地面/外部力：GRF、力矩、接触点，可来自力板/鞋垫或估计（如 Vicon + 动力平台）
- 机器人传感器：关节编码器、力矩命令、电流、IMU、足底力、接触状态、关节温度/驱动电压
- 控制器输出：基准策略命令、状态估计、延迟时间戳、控制周期 jitter，必要时记录观测向量
- 参考轨迹：人体→机器人 DOF 映射（插值、逆运动学/形体缩放）、末端路径、期望力
- 同步手段：统一 PPS/IRIG-B、NTP 或 Rosbag 时钟，必要时记录每路原始时间戳以做 post-sync

## 4. 数据对齐与预处理
- 时间同步：统一时间源、插值/重采样、延迟补偿（可记录 `Δt_state`, `Δt_cmd`, `Δt_mocap`）
- 坐标与尺度：人体模型归一化、骨架到机器人坐标变换（Pelvis 对齐、身高缩放）、末端方向矫正
- 信号清洗：低通滤波、异常检测、缺失补全、漂移矫正（IMU bias），保持原始+滤波版本
- 接触检测：根据 GRF 或动作标签标注接触相位/足序，输出 one-hot 接触向量
- 标定记录：设备标定参数、传感器漂移日志、动捕骨架质量属性、机器人关节校准文件
- 数据打包：推荐 `metadata.json` + `npz`/`hdf5`；每段数据增加 CRC 或 hash 便于校验

## 5. 残差标签构建
- 误差定义：Δ力矩、Δ关节速度、Δ末端力或能量差；可组合多目标并加入惩罚
- 计算流程：
  1. 依据 `lowcmd` 获取基准命令 `u_base`
  2. 用 `lowstate` 评估实际执行结果 `y_real`
  3. 将动捕参考 `y_ref` 映射到机器人关节或末端
  4. 计算 `residual = f(y_ref, y_real, u_base)`（例如 `residual_tau = Kp*(q_ref-q_real)+...-tau_cmd`）
- 环境上下文：地形特征、扰动信息、载荷状态加入标签，可嵌入 one-hot 或连续特征
- 样本结构：`{state, reference, env, meta} -> residual`，并保留时间戳以便序列模型
- 质量控制：设定阈值剔除残差异常（> 力矩上限 80%、>3σ），并记录被剔除索引

## 6. 训练与验证闭环
- 数据集划分：场景/执行者分层抽样，防止信息泄露
- 模型设计：残差策略网络、上下文编码（Transformer/条件编码）
- 仿真验证：先在仿真重放 → 监控稳定性、能耗指标
- 真实机验证：安全限幅、渐进部署、在线日志回收

## 7. 质量与工具链
- 数据版本管理：元数据表、采集脚本、自动校验
- 可视化工具：骨架重放、力曲线、残差分布
- 自动化脚本：批处理同步、映射、标注生成
- 回滚与追踪：可追踪到动作→样本→模型

## 8. g1_29dof_anneal_23dof 采集要点
- 关节状态：23 个 DOF 的角度/角速/力矩估计，覆盖 `dof_pos/vel/effort_limit_list` 振幅与速度
- 控制命令：根据 `control` 段（P 控制、action_scale=0.25）记录目标角、目标速、KP/KD、裁剪后力矩
- 躯干 IMU 与足端接触：骨盆/腰/足端姿态、IMU 四元数+陀螺+加速度、足底力/接触标志，呼应 `key_bodies` 与 `contact_bodies`
- 运动参考：与 `motion.motion_file` 同步的人体骨架、末端轨迹、地形标签，用于人体→机器人映射
- 随机化/扩展：`randomize_link_body_names`、`extend_config` 增强项的采样参数需入库，保证训练时可重现

## 9. listener_deltaa.py 解析摘要
- 角色：ROS2 `DataLogger` 节点，桥接 Unitree 低级状态/命令（DDS）与动捕 `/odometry` 话题，面向高频（200 Hz）缓存
- 采样内容：`low_state`（23 关节 `q/dq/tau_est` + IMU）、`low_cmd`（`q/dq/kp/kd/tau`）、`mocap`（位置、姿态、线/角速）
- 控制流程：键盘 `;` 开始、`'` 结束录制；episode 结束时写入 `humanoidverse/logs/delta_a_realdata/<exp>/<timestamp>/motion_<id>.npz`
- 数据结构：每个 `.npz` 含时间戳、关节状态与命令、IMU、动捕姿态，天然满足残差学习的“基准命令-执行反馈-人体参考”三元组
- 处理细节：
  - `buffer_update_callback` 以 200 Hz 检查最新 `low_state/low_cmd/mocap` 是否齐全，缺失会触发 warn
  - `obs_buffer` 默认可存 50*600 条，保证 200 Hz 下约 5 分钟缓存，episode 结束写入 `npz`
  - `process_and_save_data` 将 deque 展开为 numpy 数组，并以字段名写入文件，便于后续加载
  - 键盘监听采用 `pynput` 独立线程，录制期间可重复开始/停止，`motion_episode_cnt` 自动累加
  - 程序退出（Ctrl+C）会调用 `sigINT_handler`，提示已存储路径，可扩展为自动保存最后一段
- 扩展建议：可在 `LowStateHandler` 加入足底力、接触估计；在 `mocap_callback` 增加骨架关节数组；在保存函数中引入 `metadata.json`

> 快速检查清单：目标是否明确？动作覆盖是否足够？各通道是否同步？残差标签是否可解释？安全验证链路是否闭环？

